<%= form_for current_user.active_relationships.build, remote: true do |f| %>
    <%= f.hidden_field :followable_id, value: @target.id %>
    <%= f.hidden_field :followable_type, value: @target.class_name %>
    <%= f.submit "Follow", class: 'btn btn-follow' %>
<% end %>
