<%= form_for(story_comment) do |f| %>
  <% if story_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(story_comment.errors.count, "error") %> prohibited this story_comment from being saved:</h2>

      <ul>
      <% story_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :story_id %>
    <%= f.number_field :story_id %>
  </div>

  <div class="field">
    <%= f.label :comment %>
    <%= f.text_field :comment %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
